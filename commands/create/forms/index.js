const { infoForm } = require('./info');
const { statsForm } = require('./stats');
const { stringsForm } = require('./strings');

module.exports = {
	info: infoForm,
	stats: statsForm,
	strings: stringsForm,
};